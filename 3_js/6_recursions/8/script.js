const app = getById('app');
const button = getById('button');


const isSimple = (n, divider = 0) => {

};


button.addEventListener('click', () => {

	const n = getNumById('n');

	app.innerHTML = ``
});